import { BrowserRouter, Route, Routes } from "react-router-dom";

const MyRouter = () => {
    return (
        <BrowserRouter>
        <Routes>
          <Route path="/" element={}/> 
        </Routes>
      </BrowserRouter>
    )
}

export default MyRouter;